#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
cd "${repo_root}"
script/bootstrap

while IFS= read -r file; do
  node --check "${file}"
done < <(find src scripts test -type f \( -name '*.js' -o -name '*.mjs' \) | LC_ALL=C sort)
